
## POST-MIGRESSION FIX ROUND (2026-08-26, evidence-based)
1. ADMIN LOGIN "failed unexpectedly" ROOT-CAUSED & FIXED: sha256Hex() is synchronous but was chained with .then() (regression from Phase 18 stuck-button fix) -> instant TypeError before credential check. Now direct sync call in try/catch/finally. Hash re-verified: stored digest == SHA-256('Admin@2026') exactly (earlier mismatch report was a test-regex quoting error). Instructor/student login paths audited - correct patterns only.
2. FIREBASE CONNECTIVITY PROVEN, NOT ASSUMED: rebuilt live shim E2E runner (scripts/p8-shim-live.cjs, firebase@9.22.2 compat in Node) executing the REAL firestore-shim.js against LIVE Firestore under LOCKED rules -> SHIM_LIVE_E2E PASS 13/13 (doc/collection/nested/deep reads, equalTo+limit queries, set/update/push/remove roundtrips via whitelisted logs/ scratch). Non-whitelisted scratch correctly DENIED. Whitelisted write probe (admins.lastLogin) OK. Blank '--' pages in browser were stale cached pre-migration JS - Ctrl+F5 resolves; disk+served truth verified clean.
3. DUPLICATE SEARCH BARS ELIMINATED: legacy topbar search inputs (#globalSearch/#topSearch + bare decorative ones) removed from 10 pages (student dashboard/payments/profile/courses; admin dashboard/payments/courses/enrollments/students/portal-settings). ONE global search remains per portal (header button + Ctrl+K/Cmd+K). Content-level table filters intentionally KEPT (annSearch, inlineSearch, paymentSearch, studentSearchModal, instructor studentSearch). JS refs neutralized exactly (filterCourses q='', applyFilters fallback clause stripped); new window.__unmeiOpenGlobalSearch hook lets any legacy Ctrl+K handler open the real overlay. First codemod attempt broke 3 files (glued-comment brace miscount) - RESTORED from public mirror baseline then redone with exact-match edits; syntax verified after every batch.
GATES: SYNTAX_CHECK_PASS - NAV_CHECK_PASS - SYNC_CHECK_PASS - DEAD_BARS=0 (disk+served cache-busted) - SHIM_LIVE_E2E 13/13.

---
